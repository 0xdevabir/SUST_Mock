# Rule-based classification is used by default.
USE_LLM_CLASSIFIER=false

# Optional LLM settings (set USE_LLM_CLASSIFIER=true to enable)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Server
HOST=0.0.0.0
PORT=8000
